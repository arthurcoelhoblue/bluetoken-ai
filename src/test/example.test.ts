import { describe, it, expect } from "vitest";

describe("example", () => {
  it("should pass", () => {
    expect(true).toBe(true);
  });

  it("basic math works", () => {
    expect(1 + 1).toBe(2);
  });
});
